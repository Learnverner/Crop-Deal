package com.tell.resource;

import java.util.List;

import java.util.stream.Collectors;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.client.RestTemplate;
//import org.springframework.web.reactive.function.client.WebClient;
import com.tell.model.CatalogItem;
import com.tell.model.Movie;
import com.tell.model.UserRating;

@RestController
@RequestMapping("/catalog")
public class MovieCatalogResource {
@Autowired
private RestTemplate restTemplate;

@RequestMapping("/{id}")
public List<CropDetails> getCropDetails(@PathVariable("id") int id){



	   CropDetails cropDetails = restTemplate.getForObject("http://CropDetails/cropdetails" + cropId,CropDetails.class);

      
               })
               .collect(Collectors.toList());
}
}
